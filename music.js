/*
  HOW TO ADD YOUR MUSIC
  =======================
  1. Put ONE audio file inside the "music" folder
     (same folder this file points to). MP3 works best
     everywhere; M4A/OGG also work in most browsers.
  2. Change the file name below to match exactly.
  3. That's it — a small music button appears in the corner
     of the site. The visitor taps it to play/pause (browsers
     don't allow sound to start automatically, so a tap is
     required — this is normal, not a bug).

  If the file name below doesn't match a real file yet, the
  button will simply do nothing when tapped — safe to leave
  as a placeholder.
*/

const MUSIC_FILE = "music/song.mp3";
